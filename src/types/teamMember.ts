export type TeamMember = {
  id: string;
  nome: string;
  email: string;
  telefone: string;
  cargo: string;
  area: string;
};

export type TeamMemberFormState = {
  nome: string;
  email: string;
  telefone: string;
  cargo: string;
  area: string;
};

